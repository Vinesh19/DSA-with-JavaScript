// Program to Calculate the Area of a Triangle

function AreaOfTri(base, height) {
    return (base * height)/2;
}

console.log(AreaOfTri(3,5));
console.log(AreaOfTri(5,10));
console.log(AreaOfTri(8,8));